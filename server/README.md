1. Install dependencies:

```bash
cd server
npm install
```

2. Generate Prisma client:

```bash
npm run prisma:generate
```

3. Run migrations (will create tables):

```bash
npm run prisma:migrate
```

4. Start dev server:

```bash
npm run dev
```
